# Project Name : Supply Chain Execution Platform (SCEP)

## Technology Stack : Laravel 13 , LiveWire 4, FilamentPHP 5 , TailwindCSS 4 , PostgreSQL

### Create Filament NavigationGroup = Settings on top of Geographical Data

### Create Filament Resource with following the codebase that already created

### Uom

1. Create Model and Migration for Uom with Softdeletes functionality
2. Table column (id,code(string(10),unique),name(string(50)),timestamps with softdeletes functionality)
3. Create fillable on Uom
4. Create Filament resource for Uom with simple and soft delete function
5. Add Import and export FilamentPHP 5 actions on Table column

### Currency

1. Create Model and Migration for Currency with Softdeletes functionality
2. Table column (id,code(string(10),unique),name(string(150)),symbol(string(5),timestamps with softdeletes functionality)
3. Create fillable on Model
4. Create Filament resource for Currency with simple and soft delete function
5. Add Import and export FilamentPHP 5 actions on Table column

### CompanyType

1. Create Model and Migration for CompanyType with Softdeletes functionality
2. Table column (id,name(string(150)),timestamps with softdeletes functionality)
3. Create fillable on Model
4. Create Filament resource for CompanyType with simple and soft delete function
5. Add Import and export FilamentPHP 5 actions on Table column

### PaymentTerm

1. Create Model and Migration for PaymentTerm with Softdeletes functionality
2. Table column (id,code(string(10),unique),name(string(150)),days(integer,default(0),timestamps with softdeletes functionality)
3. Create fillable on Model
4. Create Filament resource for PaymentTerm with simple and soft delete function
5. Add Import and export FilamentPHP 5 actions on Table column

### OrderStatus

1. Create Model and Migration for OrderStatus with Softdeletes functionality
2. Table column (id,name(string(150)),timestamps with softdeletes functionality)
3. Create fillable on Model
4. Create Filament resource for OrderStatus with simple and soft delete function
