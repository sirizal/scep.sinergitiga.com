You are a senior Laravel 13 and Filament v5 expert.

Your task is to generate a complete implementation for a Warehouse Master Data module with the following requirements:

## Tech Stack

- Laravel 13
- Filament v5 (Admin Panel)
- Livewire v4
- PostgreSQL
- PHP 8.4+
- Use best practices (SOLID, clean code, scalable)

## Module: Warehouse Master Data

### Database Table: warehouses

Create migration with these columns:

- id (bigint, PK)
- code (string, unique, indexed)
- name (string)
- slug (string)
- type (string) // e.g. MAIN, TRANSIT, DISTRIBUTION
- address (text, nullable)
- country_id (foreign key to country table,nullable)
- province_id (foreign key to country table,nullable)
- district_id (foreign key to country table,nullable)
- sub_district_id (foreign key to country table,nullable)
- village_id (foreign key to country table,nullable)
- postal_code (string, nullable)
- latitude (decimal 10,7 nullable)
- longitude (decimal 10,7 nullable)
- is_active (boolean, default true)
- soft deletes
- created_at, updated_at

Add proper indexes where necessary.

---

## Eloquent Model: Warehouse

Requirements:

- Fillable fields
- Casts (boolean, decimal)
- Scope:
    - active()
- Relationship to reference table
- Accessor:
    - full_address (combine address with village name,sub_district name,district_name,province name,country name , postal code)
- function booted:
    - create code automatically with format WHxx (xx is running number) use db_transaction with generateCode function
    - create simple slug based on name

---

## Filament Resource: WarehouseResource

- put under navigation group Company Data

### Form

- Use appropriate components:
    - TextInput (code, name, slug , type , postal_code)
    - Select (country_id,province_id,district_id,sub_district_id,village_id) with dependent select as reference use ShipAddressForm
    - Toggle (is_active)
    - Textarea (address)
    - Map or coordinate input (latitude, longitude)
- Validation:
    - code unique only show on CreateForm , on EditForm show but readonly
    - slug unique only show on CreateForm , on EditForm show but readonly
    - required fields properly enforced

---

### Table

- Columns:
    - code, name, type, is_active (badge)
    - created_at
- Filters:
    - is_active
    - type
- Actions:
    - Edit, Delete
- Bulk Actions:
    - Delete

---

## Import Feature

Implement import using:

- Filament ImportAction OR Laravel Excel

Requirements:

- Upload CSV/XLSX
- Map columns:
  name, type, address, country,province, district,sub_district,village,postal_code,latitude, longitude, is_active
- Validate each row
- Skip or log failed rows
- Prevent duplicate code

---

## Export Feature

Implement export using:

- Filament ExportAction OR Laravel Excel

Requirements:

- Export to CSV and XLSX
- Allow filtering before export
- Include all fields

## Output Format

Generate:

1. Migration file
2. Model (Warehouse.php)
3. Filament Resource (WarehouseResource.php)
4. Import class / logic
5. Export class / logic
6. Factory
7. Seeder

Ensure code is:

- Complete
- Ready to run
- Well structured
- Commented where necessary
