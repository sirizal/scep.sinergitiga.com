You are a senior Laravel 13 and Filament v5 expert.

Your task is to generate a complete implementation for a Warehouse Master Data module with the following requirements:

## Tech Stack

- Laravel 13
- Filament v5 (Admin Panel)
- Livewire v4
- PostgreSQL
- PHP 8.4+
- Use best practices (SOLID, clean code, scalable)

## Module: Location Data

### Database Table: locations

Create migration with these columns:

- id (bigint, PK)
- code (string, unique, indexed)
- zone (string,nullable)
- aisle (string,nullable)
- rack (string,nullable)
- level (string, nullable)
- bin (string,nullable)
- warehouse_id (foreign key to warehouse table,nullable)
- type (string,nullable)
- name (string,nullable)
- max_weight (decimal 14,2 , default 0)
- max_volume (decimal 14,2 , default 0)
- is_active (boolean, default true)
- soft deletes
- created_at, updated_at

Add proper indexes where necessary.

---

## Eloquent Model: Warehouse

Requirements:

- Fillable fields
- Casts (boolean, decimal)
- Scope:
    - active()
- Relationship to reference table
- function booted:
    - create code automatically with format Warehouse_code-zone-aisle-rack-level-bin use db_transaction with generateCode function if zone,aisle,rack,level,bin is nullable , make it warehouse_code-00-00-00-00-0

---

## Filament Resource: WarehouseResource

- put under navigation group Company Data and put below warehouses

### Form

- Use appropriate components:
    - TextInput (code,zone,aisle,rack,level,bin,name,type,max_weight,max_volume)
    - Select (warehouse_id , value warehouse code searchable preload)
    - Toggle (is_active)
- Validation:
    - code unique only show on CreateForm , on EditForm show but readonly
    - required fields properly enforced

---

### Table

- Columns:
    - code, name, type, is_active (badge)
    - created_at
- Filters:
    - is_active
    - type
- Actions:
    - Edit, Delete
- Bulk Actions:
    - Delete

---

## Import Feature

Implement import using:

- Filament ImportAction OR Laravel Excel

Requirements:

- Upload CSV/XLSX
- Map columns:
  name, type, warehouse , zone , aisle , rack , level , bin , max_weight , max_volume
- Validate each row
- Skip or log failed rows
- Prevent duplicate code

---

## Export Feature

Implement export using:

- Filament ExportAction OR Laravel Excel

Requirements:

- Export to CSV and XLSX
- Allow filtering before export
- Include all fields

## Output Format

Generate:

1. Migration file
2. Model (Location.php)
3. Filament Resource (LocationResource.php)
4. Import class / logic
5. Export class / logic
6. Factory
7. Seeder

Ensure code is:

- Complete
- Ready to run
- Well structured
- Commented where necessary
