# Project Name : Supply Chain Execution Platform (SCEP)

## Technology Stack : Laravel 13 , LiveWire 4, FilamentPHP 5 , TailwindCSS 4 , PostgreSQL

### Create Filament NavigationGroup = Geographical Data

### Filament Resource

### Country

1. Create Model and Migration for Country with Softdeletes functionality
2. Table column (id,code(string(10),unique),name(string(50)),timestamps with softdeletes functionality)
3. Create fillable on Model
4. Create Filament resource for Country with simple and soft delete function
5. Add Import and export FilamentPHP 5 actions on Table column

### Province

1. Create Model and Migration for Province with Softdeletes functionality
2. Table column (id,country_id(reference to country tables),name(string(150)),timestamps with softdeletes functionality)
3. Create fillable on Model , and relationship to Country Model
4. Create Filament resource for Province with simple and soft delete function
5. On Province Form for country_id use SelectForm that can searchable
6. Add Import and export FilamentPHP 5 actions on Table column

### District

1. Create Model and Migration for District with Softdeletes functionality
2. Table column (id,province_id(reference to province tables),name(string(150)),timestamps with softdeletes functionality)
3. Create fillable on Model , and relationship to Province Model
4. Create Filament resource for District with simple and soft delete function
5. On Province Form for province_id use SelectForm that can searchable
6. Add Import and export FilamentPHP 5 actions on Table column

### SubDistrict

1. Create Model and Migration for SubDistrict with Softdeletes functionality
2. Table column (id,district_id(reference to district tables),name(string(150)),timestamps with softdeletes functionality)
3. Create fillable on Model , and relationship to District Model
4. Create Filament resource for SubDistrict with simple and soft delete function
5. On Province Form for district_id use SelectForm that can searchable
6. Add Import and export FilamentPHP 5 actions on Table column

### Village

1. Create Model and Migration for Village with Softdeletes functionality
2. Table column (id,sub_district_id(reference to sub_district tables),name(string(150)),postal_code(string(50),timestamps with softdeletes functionality)
3. Create fillable on Model , and relationship to SubDistrict Model
4. Create Filament resource for Village with simple and soft delete function
5. On Province Form for sub_district_id use SelectForm that can searchable
6. Add Import and export FilamentPHP 5 actions on Table column

### Add HasMany Relationship to Every Models that has childs
