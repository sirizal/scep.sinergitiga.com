# Project Name : Supply Chain Execution Platform (SCEP)

## Technology Stack : Laravel 13 , LiveWire 4, FilamentPHP 5 , TailwindCSS 4 , PostgreSQL

### Create Filament Resource Product position below ProductCategory Resource

### Product

1. Create Model and Migration for Product with Softdeletes functionality
2. Table column :
    - id BIGINT PRIMARY KEY AUTO_INCREMENT,
    - sku VARCHAR(50) UNIQUE,
    - name VARCHAR(255),
    - description TEXT nullable,
    - variant_code VARCHAR(20) nullable,
    - product_category_id FOREIGN KEY to table product_categories NULLABLE,
    - uom_id FOREIGN KEY to table uoms,
    - customer_product_code VARCHAR(10) NULLABLE,
    - is_active BOOLEAN DEFAULT true,
    - created_at TIMESTAMP,
    - updated_at TIMESTAMP,
    - soft_deletes functionality
3. Create fillable on Customer Product and also relationship to foreign tables
4. Create Filament resource for Product with soft delete function
5. For sku make it auto creation with format SKUxxxxxx while x are running number make it not duplicate on DB_transaction also hidden on Create Form and show Edit Form readonly
6. please also create ProductUom Model and migration (id,product_id(foreign key),uom_id (unique can not duplicate),convert_uom_id,conversion_qty(decimal(10,2))) when Product Create auto insert also to this Model uom_id and convert_uom_id are same with Product (uom_id) and coversion_qty are 1
7. Add Import and export FilamentPHP 5 actions on Table column also apply for sku creation at step 5 and 6
